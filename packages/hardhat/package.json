{
  "name": "hardhat",
  "version": "1.0.0",
  "description": "Meaningful economies of sign",
  "author": "cryptowanderer",
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.4",
    "@nomiclabs/hardhat-etherscan": "^3.0.3",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "@openzeppelin/contracts": "^4.4.2",
    "chai": "^4.3.6",
    "dotenv": "^14.3.2",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.6.9",
    "hardhat": "^2.9.9",
    "hardhat-deploy": "^0.11.11",
    "hardhat-deploy-ethers": "^0.3.0-beta.13",
    "hardhat-gas-reporter": "^1.0.8"
  },
  "scripts": {
    "clean": "yarn hardhat clean",
    "compile": "yarn hardhat compile",
    "localnode": "yarn hardhat node",
    "watch": "yarn hardhat node --watch",
    "test": "yarn hardhat test",
    "coverage": "yarn hardhat coverage",
    "accounts": "yarn hardhat accounts",
    "checksol": "yarn hardhat check",
    "deployl": "yarn hardhat --network localhost deploy --export-all ../subgraph/config/deployments.json",
    "deployg": "yarn hardhat --network goerli deploy --export-all ../subgraph/config/deployments.json",
    "deploym": "yarn hardhat --network mainnet deploy --export-all ../subgraph/config/deployments.json",
    "mintl": "yarn hardhat --network localhost mint --start 0 --end 517",
    "mintg": "yarn hardhat --network goerli mint --start 0 --end 517",
    "mintm": "yarn hardhat --network mainnet mint --start 1907 --end 2034"
  }
}
